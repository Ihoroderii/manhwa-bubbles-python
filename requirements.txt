# Core (always required)
Pillow>=8.0.0

# Cairo-based organic bubble rendering (required for the advanced overlapping bubbles)
pycairo>=1.20.0

# YOLO character detection (required to auto-detect character positions in images)
# Note: ultralytics>=8.0.152 and numpy>=1.22 require Python 3.8+
# Pin to last Python 3.7 compatible versions
ultralytics==8.0.151
opencv-python>=4.8.0
numpy==1.21.6
